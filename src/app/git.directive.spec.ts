import { GitDirective } from './git.directive';

describe('GitDirective', () => {
  it('should create an instance', () => {
    const directive = new GitDirective();
    expect(directive).toBeTruthy();
  });
});
